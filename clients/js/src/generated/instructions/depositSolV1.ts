/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DepositSolV1InstructionAccounts = {
  recipe: PublicKey | Pda;
  checklist: PublicKey | Pda;
  payer?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DepositSolV1InstructionData = {
  discriminator: Array<number>;
  reversed: boolean;
};

export type DepositSolV1InstructionDataArgs = { reversed: boolean };

export function getDepositSolV1InstructionDataSerializer(): Serializer<
  DepositSolV1InstructionDataArgs,
  DepositSolV1InstructionData
> {
  return mapSerializer<
    DepositSolV1InstructionDataArgs,
    any,
    DepositSolV1InstructionData
  >(
    struct<DepositSolV1InstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['reversed', bool()],
      ],
      { description: 'DepositSolV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [249, 138, 106, 46, 104, 57, 55, 189],
    })
  ) as Serializer<DepositSolV1InstructionDataArgs, DepositSolV1InstructionData>;
}

// Args.
export type DepositSolV1InstructionArgs = DepositSolV1InstructionDataArgs;

// Instruction.
export function depositSolV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: DepositSolV1InstructionAccounts & DepositSolV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplHybrid',
    'MPL4o4wMzndgh8T1NVDxELQCj5UQfYTYEkabX3wNKtb'
  );

  // Accounts.
  const resolvedAccounts = {
    recipe: {
      index: 0,
      isWritable: true as boolean,
      value: input.recipe ?? null,
    },
    checklist: {
      index: 1,
      isWritable: true as boolean,
      value: input.checklist ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DepositSolV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDepositSolV1InstructionDataSerializer().serialize(
    resolvedArgs as DepositSolV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
